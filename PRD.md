# 《今天谁是寿司王》产品需求文档（MVP）

| 项目 | 内容 |
|---|---|
| 产品名称 | 今天谁是寿司王 |
| 产品形态 | iOS / Android 跨端移动应用 |
| 文档状态 | MVP 需求已收敛，待视觉设计与技术评审 |
| 核心场景 | 两人在同一家寿司自助餐厅进行一场约 1 小时的实时数量比赛 |
| 核心规则 | 每吃 1 件寿司计 1 个，比赛结束时总数更多者获胜 |

## 1. 产品概述

“今天谁是寿司王”是一款面向线下双人寿司自助场景的实时计数对战应用。

两位用户无需注册账号。一人临时创建比赛并展示二维码，另一人扫码、填写昵称后加入。两人到齐即自动开始比赛。比赛过程中，双方共同维护本场寿司菜单，并分别记录自己吃过的每种寿司及数量，实时查看总比分和明细。双方确认结束后，系统自动判定冠军并生成战报。

产品的核心价值不是健康管理或饮食记录，而是将一次普通的寿司自助体验变成一场轻松、有悬念、有反馈、可留念的双人小游戏。

## 2. 产品目标

### 2.1 MVP 目标

1. 用户无需注册即可完成创建比赛、扫码加入和自动开赛。
2. 任意一方都能创建自定义寿司名称，双方实时共享并复用。
3. 双方只能为自己计数，并能实时看到对方总分及各寿司数量。
4. 断网、切后台或关闭应用后，比赛数据不丢失，并可继续比赛。
5. 双方确认结束后，系统准确判定胜负并生成本地战报。
6. 通过高质量动效和触觉反馈，让计数、追平、反超和夺冠具有游戏感。

### 2.2 验收指标

- 在网络正常的情况下，扫码加入成功率在内部测试中不低于 95%。
- 本地点击计数后的界面反馈应在 100ms 内开始。
- 在稳定网络下，计数同步到对方设备的 p95 延迟不高于 1 秒。
- 同一计数事件在断线重连后不得重复入账。
- 在覆盖 100 次断线、重连和重复提交的自动化或人工测试中，双方最终数量保持一致。
- 主流中端设备上的常规计数动画目标为 60fps。
- Beta 阶段无崩溃会话比例目标不低于 99.5%。

### 2.3 非目标

MVP 不包含：

- 注册、登录和云端账号体系
- 好友、关注和聊天系统
- 多人比赛
- 公共排行榜
- AI 寿司识别
- 拍照记录
- 餐厅或标准寿司数据库
- 卡路里、价格和营养计算
- 按寿司种类设置不同分值
- 付费或订阅功能

## 3. 目标用户与使用场景

### 3.1 目标用户

- 一起到寿司自助餐厅用餐的情侣、朋友、同事或家人
- 双方希望通过简单的数量比赛增加用餐趣味
- 双方愿意基于信任自行记录，不需要第三方裁判

### 3.2 典型场景

1. 两人到达寿司自助餐厅并打开应用。
2. 一人创建临时比赛，另一人扫码加入。
3. 寿司每隔几分钟陆续上桌。
4. 第一次出现某种寿司时，任意一方输入一个双方都能理解的名称。
5. 双方在各自手机上点击对应寿司卡片，为自己加一。
6. 双方随时查看总比分和各寿司明细。
7. 用餐结束后，一方发起结束，另一方确认。
8. 系统展示冠军、最终比分和本场战报。

## 4. 名词定义

| 名词 | 定义 |
|---|---|
| 比赛 | 两位临时用户之间的一次寿司计数对战 |
| 创建者 | 创建临时比赛并展示二维码的用户 |
| 加入者 | 扫描二维码加入比赛的用户 |
| 寿司签 | 代表一种自定义寿司的独立悬浮卡片 |
| 计数事件 | 某位用户对某个寿司执行的加一或减一操作 |
| 操作记录 | 添加、改名、合并、加一、减一等可追溯事件 |
| 已确认比赛 | 双方均同意结束并锁定最终结果的比赛 |
| 未确认比赛 | 对方长时间离线后，由一方单独结束的比赛 |

## 5. 核心比赛规则

### 5.1 计分规则

- 每吃 1 件寿司计 1 个。
- 一贯握寿司计 1 个。
- 一枚军舰寿司计 1 个。
- 卷寿司切成若干可独立食用的小件时，每件计 1 个。
- 刺身、炸物、甜品和饮料默认不计入。
- 所有寿司权重相同，不设置额外分值。
- 最终总数较多者获胜；数量相同则为平局。

### 5.2 信任规则

- 每位用户只能直接增加或减少自己的数量。
- 用户不能修改对方的计数。
- 产品不使用照片、AI 或第三方进行验证。
- 所有修改均保留操作记录，并对双方可见。

## 6. 核心用户流程

### 6.1 创建比赛

1. 用户打开应用。
2. 首页展示“创建比赛”和“扫码加入”。
3. 用户点击“创建比赛”。
4. 用户填写自己的昵称，昵称必填。
5. 系统创建一个最多容纳两人的临时房间。
6. 比赛名称固定为“今天谁是寿司王”，用户不可修改。
7. 系统展示二维码和等待状态。

### 6.2 加入比赛

1. 加入者使用应用内扫码或系统相机扫描二维码。
2. 已安装应用时，通过深度链接进入加入页面。
3. 未安装应用时，跳转对应应用商店；安装后应尽可能恢复原加入链接。
4. 加入者填写自己的昵称，昵称必填。
5. 系统确认房间有效且仍有空位。
6. 加入成功后，两人自动进入比赛页面，比赛立即开始，无需额外点击“开始”。

### 6.3 进行比赛

1. 比赛页面顶部固定展示双方昵称、总分、领先者和分差。
2. 初始共享寿司菜单为空。
3. 任意一方均可创建一个自定义名称的寿司签。
4. 新寿司签实时出现在双方设备上。
5. 用户点击某张寿司签的计数按钮，只增加自己的数量。
6. 双方实时看到总比分和每张寿司签上的双方数量。
7. 用户可立即撤销刚才的操作，也可从寿司签操作菜单修正较早的错误。

### 6.4 结束比赛

1. 任意一方点击“结束比赛”。
2. 对方收到结束确认请求。
3. 发起方在等待期间可以撤回请求。
4. 对方确认后，比赛立即锁定，双方不能继续计数。
5. 系统根据最终总数判定冠军或平局。
6. 双方进入结果页面并在本地保存战报。
7. 如果对方持续离线 10 分钟，在线方可以单方面结束；该结果标记为“未经双方确认”。

## 7. 功能需求

### 7.1 首页与匿名身份

| 编号 | 需求 |
|---|---|
| FR-001 | 首页必须提供“创建比赛”和“扫码加入”两个主要入口。 |
| FR-002 | 创建者和加入者均必须填写本场昵称。 |
| FR-003 | 昵称仅在本场比赛及本地历史记录中使用，不创建账号。 |
| FR-004 | 设备应生成匿名设备标识，用于断线重连，不作为公开账号。 |
| FR-005 | 应保存用户最近一次使用的昵称，并允许用户修改后再进入新比赛。 |

### 7.2 临时房间与二维码

| 编号 | 需求 |
|---|---|
| FR-010 | 每个房间最多容纳两名玩家。 |
| FR-011 | 二维码中使用不可预测的随机房间令牌，不暴露连续房间编号。 |
| FR-012 | 等待房间超过 2 小时无人加入时自动失效。 |
| FR-013 | 第三人扫码时应明确提示“比赛人数已满”。 |
| FR-014 | 房间失效时应明确提示并引导重新创建。 |
| FR-015 | 加入者完成昵称提交后，双方自动进入比赛页面。 |

### 7.3 寿司签

| 编号 | 需求 |
|---|---|
| FR-020 | 任意一方均可输入简单名称创建寿司签。 |
| FR-021 | 创建寿司签时仅要求名称，不要求照片、分类或描述。 |
| FR-022 | 创建寿司签不会自动增加创建者的数量。 |
| FR-023 | 新寿司签默认排列在列表最前面。 |
| FR-024 | 已存在的寿司签不会因计数而改变位置，避免误触。 |
| FR-025 | 创建时应检测完全相同或高度相似的名称，并提示可能重复。 |
| FR-026 | 用户仍可忽略重复提示并继续创建。 |
| FR-027 | 双方均可修改寿司名称，修改行为进入操作记录。 |
| FR-028 | 重复寿司签可以合并，双方在两张卡片上的数量分别相加。 |
| FR-029 | 已产生计数的寿司签不可直接删除，只能合并或标记无效。 |
| FR-030 | 当寿司签较多时，页面应提供名称搜索。 |

### 7.4 计数与撤销

| 编号 | 需求 |
|---|---|
| FR-040 | 每张寿司签必须提供清晰、易点击的“+1”计数按钮。 |
| FR-041 | 点击后应立即乐观更新本地寿司数量和总分。 |
| FR-042 | 点击只影响当前用户自己的数量。 |
| FR-043 | 每次点击必须生成唯一事件 ID，以支持幂等同步。 |
| FR-044 | 点击成功后应提供可操作的“撤销”提示。 |
| FR-045 | 撤销同时修正对应寿司数量和总分。 |
| FR-046 | 用户可通过寿司签操作菜单执行“减少一个”。 |
| FR-047 | 数量不得小于 0。 |
| FR-048 | 连续点击不得被前一次动画阻塞。 |

### 7.5 实时比分

| 编号 | 需求 |
|---|---|
| FR-050 | 比赛页面顶部固定显示双方昵称和总分。 |
| FR-051 | 系统应实时显示领先者和分差。 |
| FR-052 | 平分时显示“当前平局”。 |
| FR-053 | 每张寿司签显示自己和对方在该寿司上的数量。 |
| FR-054 | 收到对方事件时，应更新对应寿司签及顶部总分。 |
| FR-055 | 对方离线时应显示离线状态，但不得阻止当前用户继续计数。 |

### 7.6 操作记录

| 编号 | 需求 |
|---|---|
| FR-060 | 系统记录加一、减一、撤销、创建、改名、合并和结束请求。 |
| FR-061 | 双方可以查看本场操作记录。 |
| FR-062 | 每条记录至少包含操作者、动作、目标寿司和时间。 |
| FR-063 | 操作记录默认折叠，不占据主比赛页面核心空间。 |

### 7.7 本地历史与战报

| 编号 | 需求 |
|---|---|
| FR-070 | 已结束比赛应分别保存在双方设备本地。 |
| FR-071 | 本地历史不依赖账号，卸载应用或清除数据后可能丢失。 |
| FR-072 | 战报展示日期、持续时间、双方昵称、最终比分和冠军。 |
| FR-073 | 战报展示双方各寿司数量、吃得最多的寿司、最大领先和反超次数。 |
| FR-074 | 用户可以将战报生成图片并通过系统分享面板分享。 |
| FR-075 | 完成后的服务器房间数据默认保留 7 天，之后自动删除。 |

## 8. 页面与信息架构

### 8.1 页面清单

1. 启动与首页
2. 创建比赛昵称页
3. 等待加入与二维码页
4. 扫码页
5. 加入比赛昵称页
6. 比赛主页面
7. 添加寿司状态
8. 寿司签操作菜单
9. 操作记录页
10. 结束比赛确认状态
11. 比赛结果与战报页
12. 本地历史列表
13. 历史战报详情

### 8.2 比赛主页面

页面分为三个主要区域：

1. 固定总比分区
2. 可滚动寿司签区域
3. 添加寿司入口

示意：

```text
┌────────────────────────────┐
│ 阿杰  18       VS       16  小美 │
│          阿杰领先 2 个          │
└────────────────────────────┘

本场寿司 · 6 种               搜索

╭────────────────────────────╮
│ 炙烧三文鱼                    │
│                              │
│ 我的数量 4        对方数量 3   │
│                         ＋1   │
╰────────────────────────────╯

╭────────────────────────────╮
│ 鳗鱼握寿司                    │
│                              │
│ 我的数量 2        对方数量 5   │
│                         ＋1   │
╰────────────────────────────╯

              ＋ 添加新寿司
```

## 9. 视觉与交互设计要求

### 9.1 视觉方向

- 关键词：高级、俏皮、竞技、轻松、具有寿司主题，但不幼稚。
- 寿司签采用独立悬浮卡片，每张卡片具有明确层次与触摸反馈。
- 卡片之间保留足够空间，避免误触。
- 两位玩家分别使用稳定的识别色，并在比分、卡片数字和事件反馈中保持一致。
- 不依赖寿司照片或复杂插画区分内容，寿司名称必须始终清晰。
- 支持浅色和深色外观，并保证文本和关键控件对比度。
- iOS 与 Android 可共享品牌表现，但导航、返回、弹层和触觉反馈遵循各自平台规范。

### 9.2 触摸要求

- iOS 触摸目标不小于 44 × 44 pt。
- Android 触摸目标不小于 48 × 48 dp。
- “+1”是每张寿司签最主要的交互控件。
- 添加、结束、撤销等操作必须具有清晰的文字标签，不能只依赖图标。

## 10. 动效与触觉规范

### 10.1 动效原则

- 页面保持相对安静，将动效预算集中在计数、对手得分、反超、撤销和夺冠。
- 普通反馈控制在 100–400ms。
- 动画不得阻止连续计数。
- 动画必须表达状态变化，而不是持续进行无意义装饰。
- 支持 iOS“减弱动态效果”和 Android“移除动画”。

### 10.2 本地计数

点击“+1”时：

1. 按钮产生约 100ms 的按压缩放反馈。
2. 设备触发一次轻触觉反馈。
3. 当前寿司数量使用数字滚动效果加一。
4. 一枚轻量寿司筹码从卡片飞向顶部当前用户总分。
5. 顶部总分同步滚动增加。
6. 当前玩家识别色在卡片上短暂扫过。
7. 底部显示“已记录某寿司 +1 · 撤销”。

### 10.3 对手计数

收到对方计数时：

- 对方寿司数量滚动增加。
- 对方识别色在对应卡片上短暂闪过。
- 顶部对方总分轻微脉冲。
- 可以使用轻微触觉提示，但不得播放默认声音或打断操作。

### 10.4 反超

反超是产品的招牌动效：

- 顶部领先状态进行平滑交接。
- 显示约 1 秒的“反超！”提示。
- 领先方获得一次更有力度的触觉反馈。
- 比分中心可释放少量寿司主题粒子。
- 动画结束后恢复稳定状态，不持续闪烁。

### 10.5 撤销

- 寿司数量和总分反向滚动。
- 寿司筹码沿简化轨迹返回卡片。
- 卡片短暂显示“已撤销”状态。
- 撤销不弹出确认框。

### 10.6 比赛结束

- 寿司签降低视觉权重。
- 总比分区扩大并锁定最终数字。
- 显示冠军或平局。
- 冠军场景播放一次短促的寿司主题庆祝粒子和触觉反馈。
- 平局场景让双方识别色在中间达到视觉平衡，不播放冠军效果。

### 10.7 减少动态效果

启用系统减少动态效果后：

- 筹码飞行动画替换为卡片和比分的短暂颜色变化。
- 大范围位移替换为交叉淡化或即时切换。
- 粒子效果关闭。
- 所有信息和反馈仍必须完整可理解。

## 11. 网络、同步与数据一致性

### 11.1 同步原则

- 计数采用本地乐观更新，网络请求在后台完成。
- 服务端是比赛共享状态的最终一致来源。
- 所有改变以不可重复的事件形式提交。
- 服务端按事件 ID 去重。
- 客户端重连后提交尚未确认的本地事件，并拉取缺失的远端事件。

### 11.2 冲突处理

- 双方同时对不同寿司计数时，事件分别合并。
- 双方同时对同一寿司计数时，双方各自数量分别增加。
- 改名与计数同时发生时，计数绑定寿司 ID，不绑定寿司名称。
- 合并发生时，旧寿司 ID 通过映射归入目标寿司，迟到事件不得丢失。
- 客户端显示与服务端不一致时，静默校准；若变化明显，应显示“比分已同步”。

### 11.3 重连

- 应用切后台、进程被杀或网络中断后，设备保存当前房间令牌和未确认事件。
- 再次打开应用时，优先恢复未结束比赛。
- 房间仍有效时自动重连。
- 房间已失效时，保留本地记录并明确告知用户无法继续实时同步。

## 12. 状态与异常处理

| 场景 | 产品行为 |
|---|---|
| 二维码无效或过期 | 提示比赛已失效，并提供返回首页操作 |
| 房间已满 | 提示该比赛已有两人 |
| 加入时网络失败 | 保留昵称，允许重试 |
| 创建寿司时断网 | 先在本地显示待同步卡片，恢复后自动提交 |
| 名称为空 | 禁止提交并提示填写寿司名称 |
| 名称过长 | 限制为 30 个字符，并在输入时显示剩余字符 |
| 连续快速点击 | 每次均产生独立事件，动画可叠加但不得阻塞 |
| 数量减到 0 | 禁止继续减少 |
| 对方离线 | 显示离线状态，自己仍可继续记录 |
| 双方同时发起结束 | 合并为同一个结束流程 |
| 一方拒绝结束 | 恢复比赛状态并通知发起方 |
| 比赛结束后收到迟到事件 | 按结束锁定时间判断；锁定后的新事件不计入结果 |
| 应用被卸载 | 本地历史可能丢失，MVP 不提供恢复 |

## 13. 数据模型概要

### 13.1 Match

- `id`
- `title`，固定为“今天谁是寿司王”
- `room_token`
- `status`：waiting / active / end_pending / completed / unconfirmed / expired
- `created_at`
- `started_at`
- `ended_at`
- `end_requested_by`
- `winner_player_id`，平局时为空

### 13.2 Player

- `id`
- `match_id`
- `anonymous_device_id`
- `nickname`
- `role`：creator / joiner
- `color_role`
- `connection_status`
- `joined_at`

### 13.3 SushiType

- `id`
- `match_id`
- `name`
- `created_by`
- `created_at`
- `updated_at`
- `merged_into_id`，未合并时为空
- `status`：active / merged / invalid

### 13.4 MatchEvent

- `event_id`
- `match_id`
- `player_id`
- `sushi_type_id`
- `event_type`
- `quantity_delta`
- `client_created_at`
- `server_received_at`
- `metadata`

## 14. 隐私与安全

- MVP 不收集真实姓名、手机号、邮箱或通讯录。
- 昵称由用户自行填写，并仅用于比赛展示。
- 二维码房间令牌必须足够随机，避免被枚举。
- 服务器仅在实现比赛、同步和临时战报所需的范围内保存数据。
- 完成比赛的服务器数据默认 7 天后删除。
- 本地历史由用户设备保存。
- 分享战报前由用户主动触发系统分享面板。

## 15. 分析事件

在不采集直接身份信息的前提下记录：

- 创建比赛
- 展示二维码
- 扫码成功 / 失败及失败原因
- 加入比赛
- 自动开始比赛
- 创建寿司签
- 执行计数
- 执行撤销
- 发生断线和重连
- 发起、确认、拒绝或撤回结束
- 完成比赛
- 生成和分享战报

不上传用户输入的具体寿司名称作为通用分析属性。

## 16. MVP 验收场景

### 场景 A：正常完成比赛

1. A 填写昵称并创建比赛。
2. B 扫码填写昵称。
3. 双方自动进入比赛。
4. A 创建“炙烧三文鱼”。
5. 双方分别计数。
6. 双方实时看到一致比分。
7. A 发起结束，B 确认。
8. 系统正确判定冠军并保存战报。

### 场景 B：断网恢复

1. 比赛进行中，A 断网。
2. A 离线记录多个寿司。
3. B 继续正常记录。
4. A 恢复网络。
5. 双方最终寿司明细和总分一致。
6. 所有离线事件均只入账一次。

### 场景 C：误操作

1. A 点击某寿司加一。
2. A 立即撤销。
3. 双方对应寿司和总分均恢复。
4. 操作记录可追溯此次加一和撤销。

### 场景 D：重复寿司

1. A 创建“炙烧三文鱼”。
2. B 尝试创建同名寿司。
3. 系统提示可能重复。
4. 如果仍然创建，双方可在之后合并。
5. 合并后双方数量正确相加。

### 场景 E：一方离线结束

1. A 发起结束。
2. B 持续离线超过 10 分钟。
3. A 选择单方面结束。
4. 系统生成结果并标记为“未经双方确认”。

## 17. 默认策略与待验证项

以下内容不是未决需求，MVP 可按默认值实现，但应在测试中验证：

| 项目 | 默认策略 |
|---|---|
| 等待房间有效期 | 2 小时 |
| 单方面结束等待时间 | 对方持续离线 10 分钟 |
| 完成比赛服务端保留期 | 7 天 |
| 寿司名称最大长度 | 30 个字符 |
| 寿司签默认排序 | 新创建的在最前，计数不改变位置 |
| 本地历史保留 | 用户不主动删除时持续保留 |
| 比赛声音 | 默认无声音，以视觉和触觉反馈为主 |

## 18. 后续版本方向

- 可选账号与跨设备历史
- 固定好友或情侣关系
- 多人房间和团队赛
- 自定义比赛名称
- 战绩排行榜
- 不同寿司分值模式
- 品类收集模式
- 餐厅主题皮肤
- 更多战报模板
- AI 辅助识别寿司名称
- Apple App Clip / Android Instant App 式免安装加入体验

